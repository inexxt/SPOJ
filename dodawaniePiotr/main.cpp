#include <stdio.h>
using namespace std;

int main()
{
    char tab[1000];
    int t;
    scanf("%d", &t);
    while (
    while (t)
    {
        scanf("%d", &a);

        while (!war)
        {

        }
    }
    return 0;
}
