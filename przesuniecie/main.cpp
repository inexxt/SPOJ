#include <cstdio>

using namespace std;

int main()
{
    int a, b, t;
    scanf("%d", &t);
    while(t)
    {
        scanf("%d""%d", &v1, &v2);
        printf("%d\n", 2*v1*v2/(v1+v2));
        t--;
    }

    return 0;
}
