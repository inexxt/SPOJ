#include <stdio.h>

using namespace std;
char b[1000], b2[1000];
int main()
{
    int t, i=0;
    scanf("%d", &t);
    while(t)
    {
        i=0;
        while(n!=EOF)
        {
            for()
            {
                n=scanf("%c%c", &b[i], &b2[i]);
                i++;
            }
        }

        for(int i2=0; i2<i/2; i2++)
        {
            printf("%c%c\n", b[i2], b2[i2]);
        }
        t--;
    }
    return 0;
}
